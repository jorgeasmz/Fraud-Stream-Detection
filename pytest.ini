[pytest]
testpaths = tests
pythonpath = .
addopts = -v -m "not postgres" --cov=ingest --cov=features --cov=detect --cov=evaluation --cov=stream --cov=api --cov=db --cov-report=term-missing
markers =
    postgres: needs a live PostgreSQL, run in CI against a service container
